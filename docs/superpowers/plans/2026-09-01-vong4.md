# Vòng 4 — Đăng nhập Google (OAuth) & Dark mode — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thêm đăng nhập Google (tự liên kết theo email) và dark mode (light/dark/system) cho nền tảng.

**Architecture:** Giữ nguyên NextAuth v5 JWT session (không thêm DB adapter). Google provider upsert User trong `jwt` callback qua hàm thuần `resolveOAuthUser`. Logic đăng nhập mật khẩu tách thành hàm thuần `resolveCredentials` (từ chối tài khoản chỉ-Google). Dark mode dùng `next-themes` với CSS `.dark` đã có sẵn.

**Tech Stack:** Next.js 16, React 19, NextAuth v5 (beta.32), Prisma 6, next-themes 0.4.6, Vitest, Tailwind v4 (design tokens).

## Global Constraints

- Schema sync bằng `npm run db:push` (Prisma db push, **không** dùng migrations).
- Prisma pin v6 — không nâng cấp.
- Hàm logic thuần: dependency injection, **không** import `prisma`/`auth` trực tiếp; test bằng `vi.fn().mockResolvedValue(...)`.
- Chỉ dùng Tailwind design token (`bg-background`, `text-foreground`, `text-muted-foreground`, `border-border`, `bg-card`, `bg-primary`…). Không thêm màu cứng mới.
- **Không sửa** các component xem trước CV (`components/cv/preview/*`, `components/cv/CvPreview.tsx`, `app/cv/[id]/evaluate/EvaluateClient.tsx`) — chúng cố tình nền sáng để khớp PDF (in ra giấy trắng). Đây KHÔNG phải lỗi dark mode.
- Toàn bộ chữ UI bằng tiếng Việt.
- OAuth: tự động liên kết theo email; JWT không adapter; user Google mới có `role = CANDIDATE`, `passwordHash = null`.
- Dark mode: `next-themes`, `attribute="class"`, `defaultTheme="system"`, `enableSystem`.
- Toàn bộ test hiện có phải xanh (326 test). Không viết test giả cho UI/toggle.

---

### Task 1: Schema `passwordHash` optional + hàm thuần `resolveCredentials`

Cho phép user chỉ-Google (không mật khẩu) tồn tại, và tách logic đăng nhập mật khẩu thành hàm thuần từ chối các tài khoản đó.

**Files:**
- Modify: `prisma/schema.prisma:20` (`passwordHash String` → `passwordHash String?`)
- Create: `lib/auth/credentials.ts`
- Test: `lib/auth/__tests__/credentials.test.ts`
- Modify: `auth.ts:12-34` (authorize dùng `resolveCredentials`)

**Interfaces:**
- Produces:
  - `type AuthedUser = { id: string; email: string; name: string; role: "CANDIDATE" | "RECRUITER" | "ADMIN" }`
  - `type CredentialsUser = AuthedUser & { passwordHash: string | null }`
  - `type ResolveCredentialsDeps = { findByEmail: (email: string) => Promise<CredentialsUser | null>; verify: (plain: string, hash: string) => Promise<boolean> }`
  - `resolveCredentials(email: string, password: string, deps: ResolveCredentialsDeps): Promise<AuthedUser | null>`

- [ ] **Step 1: Đổi schema `passwordHash` thành optional**

Trong `prisma/schema.prisma`, model `User`, đổi dòng:
```prisma
  passwordHash String
```
thành:
```prisma
  passwordHash String?
```

- [ ] **Step 2: Đồng bộ schema + regenerate client**

Run: `npm run db:push`
Expected: "Your database is now in sync with your Prisma schema" và Prisma Client được generate lại.
(Nếu DB không kết nối được: chạy `npx prisma generate` để cập nhật type của client — đủ cho typecheck/test; nhớ chạy `npm run db:push` khi có DB.)

- [ ] **Step 3: Viết test thất bại cho `resolveCredentials`**

Tạo `lib/auth/__tests__/credentials.test.ts`:
```typescript
import { describe, it, expect, vi } from "vitest";
import { resolveCredentials } from "../credentials";

const baseUser = {
  id: "u1",
  email: "a@b.com",
  name: "Manh",
  role: "CANDIDATE" as const,
  passwordHash: "hashed",
};

const deps = (over: Partial<ReturnType<typeof mk>> = {}) => ({ ...mk(), ...over });
function mk() {
  return {
    findByEmail: vi.fn().mockResolvedValue(baseUser),
    verify: vi.fn().mockResolvedValue(true),
  };
}

describe("resolveCredentials", () => {
  it("tra ve user khi mat khau dung", async () => {
    const d = deps();
    const r = await resolveCredentials("a@b.com", "matkhau123", d);
    expect(r).toEqual({ id: "u1", email: "a@b.com", name: "Manh", role: "CANDIDATE" });
  });

  it("null khi khong tim thay user", async () => {
    const d = deps({ findByEmail: vi.fn().mockResolvedValue(null) });
    const r = await resolveCredentials("x@y.com", "matkhau123", d);
    expect(r).toBeNull();
    expect(d.verify).not.toHaveBeenCalled();
  });

  it("null khi tai khoan chi-Google (passwordHash null)", async () => {
    const d = deps({
      findByEmail: vi.fn().mockResolvedValue({ ...baseUser, passwordHash: null }),
    });
    const r = await resolveCredentials("a@b.com", "matkhau123", d);
    expect(r).toBeNull();
    expect(d.verify).not.toHaveBeenCalled();
  });

  it("null khi mat khau sai", async () => {
    const d = deps({ verify: vi.fn().mockResolvedValue(false) });
    const r = await resolveCredentials("a@b.com", "sai", d);
    expect(r).toBeNull();
  });
});
```

- [ ] **Step 4: Chạy test để xác nhận FAIL**

Run: `npx vitest run lib/auth/__tests__/credentials.test.ts`
Expected: FAIL — "Cannot find module '../credentials'".

- [ ] **Step 5: Viết `lib/auth/credentials.ts`**

```typescript
export type AuthedUser = {
  id: string;
  email: string;
  name: string;
  role: "CANDIDATE" | "RECRUITER" | "ADMIN";
};

export type CredentialsUser = AuthedUser & { passwordHash: string | null };

export type ResolveCredentialsDeps = {
  findByEmail: (email: string) => Promise<CredentialsUser | null>;
  verify: (plain: string, hash: string) => Promise<boolean>;
};

export async function resolveCredentials(
  email: string,
  password: string,
  deps: ResolveCredentialsDeps,
): Promise<AuthedUser | null> {
  const user = await deps.findByEmail(email);
  if (!user) return null;
  if (!user.passwordHash) return null; // tài khoản chỉ-Google, không cho đăng nhập mật khẩu
  const ok = await deps.verify(password, user.passwordHash);
  if (!ok) return null;
  return { id: user.id, email: user.email, name: user.name, role: user.role };
}
```

- [ ] **Step 6: Chạy test để xác nhận PASS**

Run: `npx vitest run lib/auth/__tests__/credentials.test.ts`
Expected: PASS (4 test).

- [ ] **Step 7: Nối `authorize` trong `auth.ts` dùng `resolveCredentials`**

Trong `auth.ts`, thêm import ở đầu file (sau import `verifyPassword`):
```typescript
import { resolveCredentials } from "@/lib/auth/credentials";
```
Thay toàn bộ thân `authorize` (dòng 14-33) thành:
```typescript
      authorize: async (creds, request) => {
        const email = creds?.email as string | undefined;
        const password = creds?.password as string | undefined;
        if (!email || !password) return null;

        const ip = getClientIp(request as Request | undefined);
        const ok = await checkRateLimit("login", `${ip}:${email}`);
        if (!ok) {
          console.warn("[auth] login bị rate-limit:", email);
          return null; // trả lỗi đồng nhất, không tiết lộ bị khoá
        }

        return resolveCredentials(email, password, {
          findByEmail: (e) =>
            prisma.user.findUnique({
              where: { email: e },
              select: { id: true, email: true, name: true, role: true, passwordHash: true },
            }),
          verify: verifyPassword,
        });
      },
```

- [ ] **Step 8: Chạy toàn bộ test + typecheck**

Run: `npx vitest run`
Expected: PASS (330 test — 326 cũ + 4 mới).
Run: `npx tsc --noEmit`
Expected: không lỗi.

- [ ] **Step 9: Commit**

```bash
git add prisma/schema.prisma lib/auth/credentials.ts lib/auth/__tests__/credentials.test.ts auth.ts
git commit -m "feat(auth): passwordHash optional + resolveCredentials (reject Google-only accounts)"
```

---

### Task 2: Hàm thuần `resolveOAuthUser`

Logic liên kết/tạo User khi đăng nhập Google, testable độc lập.

**Files:**
- Create: `lib/auth/oauth.ts`
- Test: `lib/auth/__tests__/oauth.test.ts`

**Interfaces:**
- Produces:
  - `type OAuthRole = "CANDIDATE" | "RECRUITER" | "ADMIN"`
  - `type OAuthUser = { id: string; role: OAuthRole }`
  - `type ResolveOAuthDeps = { findByEmail: (email: string) => Promise<OAuthUser | null>; createUser: (email: string, name: string) => Promise<OAuthUser> }`
  - `resolveOAuthUser(email: string, name: string, deps: ResolveOAuthDeps): Promise<OAuthUser>`

- [ ] **Step 1: Viết test thất bại**

Tạo `lib/auth/__tests__/oauth.test.ts`:
```typescript
import { describe, it, expect, vi } from "vitest";
import { resolveOAuthUser } from "../oauth";

describe("resolveOAuthUser", () => {
  it("lien ket: tra ve user co san, khong tao moi", async () => {
    const findByEmail = vi.fn().mockResolvedValue({ id: "u0", role: "RECRUITER" });
    const createUser = vi.fn();
    const r = await resolveOAuthUser("a@b.com", "Manh", { findByEmail, createUser });
    expect(r).toEqual({ id: "u0", role: "RECRUITER" });
    expect(createUser).not.toHaveBeenCalled();
  });

  it("tao moi voi role CANDIDATE khi email chua ton tai", async () => {
    const findByEmail = vi.fn().mockResolvedValue(null);
    const createUser = vi.fn().mockResolvedValue({ id: "u1", role: "CANDIDATE" });
    const r = await resolveOAuthUser("new@b.com", "Tan", { findByEmail, createUser });
    expect(r).toEqual({ id: "u1", role: "CANDIDATE" });
    expect(createUser).toHaveBeenCalledWith("new@b.com", "Tan");
  });
});
```

- [ ] **Step 2: Chạy test để xác nhận FAIL**

Run: `npx vitest run lib/auth/__tests__/oauth.test.ts`
Expected: FAIL — "Cannot find module '../oauth'".

- [ ] **Step 3: Viết `lib/auth/oauth.ts`**

```typescript
export type OAuthRole = "CANDIDATE" | "RECRUITER" | "ADMIN";
export type OAuthUser = { id: string; role: OAuthRole };

export type ResolveOAuthDeps = {
  findByEmail: (email: string) => Promise<OAuthUser | null>;
  createUser: (email: string, name: string) => Promise<OAuthUser>;
};

export async function resolveOAuthUser(
  email: string,
  name: string,
  deps: ResolveOAuthDeps,
): Promise<OAuthUser> {
  const existing = await deps.findByEmail(email);
  if (existing) return existing; // liên kết theo email
  return deps.createUser(email, name);
}
```

- [ ] **Step 4: Chạy test để xác nhận PASS**

Run: `npx vitest run lib/auth/__tests__/oauth.test.ts`
Expected: PASS (2 test).

- [ ] **Step 5: Commit**

```bash
git add lib/auth/oauth.ts lib/auth/__tests__/oauth.test.ts
git commit -m "feat(auth): resolveOAuthUser pure logic (link-by-email or create CANDIDATE)"
```

---

### Task 3: Google provider trong `auth.ts` + `.env.example`

Thêm provider Google, chặn khi thiếu email, và upsert User trong `jwt` callback.

**Files:**
- Modify: `auth.ts` (import Google + resolveOAuthUser; thêm provider; thêm `signIn` callback; sửa `jwt` callback)
- Modify: `.env.example` (thêm `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`)

**Interfaces:**
- Consumes: `resolveOAuthUser` (Task 2), `prisma`.

- [ ] **Step 1: Thêm import Google provider + resolveOAuthUser**

Trong `auth.ts`, thêm sau `import Credentials ...` (dòng 2):
```typescript
import Google from "next-auth/providers/google";
```
Và sau import `resolveCredentials` (thêm ở Task 1):
```typescript
import { resolveOAuthUser } from "@/lib/auth/oauth";
```

- [ ] **Step 2: Thêm Google vào mảng `providers`**

Sau khối `Credentials({...})` trong `providers: [...]`, thêm:
```typescript
    Google({
      clientId: process.env.GOOGLE_CLIENT_ID,
      clientSecret: process.env.GOOGLE_CLIENT_SECRET,
    }),
```

- [ ] **Step 3: Thêm `signIn` callback + sửa `jwt` callback**

Trong `callbacks: { ... }`, thêm `signIn` (trước `jwt`) và thay `jwt` hiện tại:
```typescript
    async signIn({ account, profile }) {
      if (account?.provider === "google") {
        return Boolean(profile?.email); // chặn nếu Google không trả email
      }
      return true;
    },
    async jwt({ token, user, account, profile }) {
      if (account?.provider === "google" && profile?.email) {
        const resolved = await resolveOAuthUser(
          profile.email as string,
          (profile.name as string) || (profile.email as string),
          {
            findByEmail: (email) =>
              prisma.user.findUnique({ where: { email }, select: { id: true, role: true } }),
            createUser: (email, name) =>
              prisma.user.create({
                data: { email, name, role: "CANDIDATE" },
                select: { id: true, role: true },
              }),
          },
        );
        token.id = resolved.id;
        token.role = resolved.role;
      } else if (user) {
        token.id = (user as { id: string }).id;
        token.role = (user as { role?: "CANDIDATE" | "RECRUITER" | "ADMIN" }).role;
      }
      return token;
    },
```
(Giữ nguyên `session` callback bên dưới.)

- [ ] **Step 4: Thêm biến môi trường vào `.env.example`**

Sau dòng `AUTH_SECRET=...` trong `.env.example`, thêm:
```
# Google OAuth (đăng nhập với Google). Tạo tại console.cloud.google.com → APIs & Services → Credentials.
# Authorized redirect URI: http://localhost:3000/api/auth/callback/google (dev) + domain production.
GOOGLE_CLIENT_ID="....apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="GOCSPX-..."
```

- [ ] **Step 5: Chạy test + typecheck**

Run: `npx vitest run`
Expected: PASS (330 test, không đổi so với Task 2 — đây là task nối dây).
Run: `npx tsc --noEmit`
Expected: không lỗi.

- [ ] **Step 6: Commit**

```bash
git add auth.ts .env.example
git commit -m "feat(auth): Google OAuth provider with link-by-email in jwt callback"
```

---

### Task 4: Nút "Đăng nhập với Google" ở `/login` và `/register`

**Files:**
- Create: `components/GoogleSignInButton.tsx`
- Modify: `app/login/page.tsx` (thêm divider + nút sau form)
- Modify: `app/register/page.tsx` (thêm divider + nút sau form)

**Interfaces:**
- Produces: `GoogleSignInButton` (default export, prop `label?: string`).

- [ ] **Step 1: Tạo `components/GoogleSignInButton.tsx`**

```tsx
"use client";

import { signIn } from "next-auth/react";
import { Button } from "@/components/ui/button";

function GoogleIcon() {
  return (
    <svg className="h-4 w-4" viewBox="0 0 24 24" aria-hidden="true">
      <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92a5.06 5.06 0 0 1-2.2 3.32v2.76h3.56c2.08-1.92 3.28-4.74 3.28-8.09Z" />
      <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.56-2.76c-.98.66-2.24 1.06-3.72 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84A11 11 0 0 0 12 23Z" />
      <path fill="#FBBC05" d="M5.84 14.11a6.6 6.6 0 0 1 0-4.22V7.05H2.18a11 11 0 0 0 0 9.9l3.66-2.84Z" />
      <path fill="#EA4335" d="M12 4.75c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 1.46 14.97.5 12 .5A11 11 0 0 0 2.18 7.05l3.66 2.84C6.71 6.68 9.14 4.75 12 4.75Z" />
    </svg>
  );
}

export default function GoogleSignInButton({
  label = "Đăng nhập với Google",
}: {
  label?: string;
}) {
  return (
    <Button
      type="button"
      variant="outline"
      className="w-full"
      onClick={() => signIn("google", { redirectTo: "/dashboard" })}
    >
      <GoogleIcon />
      {label}
    </Button>
  );
}
```

- [ ] **Step 2: Thêm divider + nút vào `app/login/page.tsx`**

Thêm import sau dòng `import { Input } ...`:
```tsx
import GoogleSignInButton from "@/components/GoogleSignInButton";
```
Ngay sau thẻ đóng `</form>` (dòng 49) và trước đoạn `<p className="mt-4 ...">`, chèn:
```tsx
          <div className="my-4 flex items-center gap-3">
            <div className="h-px flex-1 bg-border" />
            <span className="text-xs text-muted-foreground">hoặc</span>
            <div className="h-px flex-1 bg-border" />
          </div>
          <GoogleSignInButton />
```

- [ ] **Step 3: Thêm divider + nút vào `app/register/page.tsx`**

Thêm import sau dòng `import { Input } ...`:
```tsx
import GoogleSignInButton from "@/components/GoogleSignInButton";
```
Ngay sau thẻ đóng `</form>` và trước đoạn `<p className="mt-4 ...">`, chèn:
```tsx
          <div className="my-4 flex items-center gap-3">
            <div className="h-px flex-1 bg-border" />
            <span className="text-xs text-muted-foreground">hoặc</span>
            <div className="h-px flex-1 bg-border" />
          </div>
          <GoogleSignInButton label="Đăng ký với Google" />
          <p className="mt-1 text-center text-xs text-muted-foreground">
            Đăng ký bằng Google sẽ tạo tài khoản Ứng viên.
          </p>
```

- [ ] **Step 4: Typecheck + lint**

Run: `npx tsc --noEmit`
Expected: không lỗi.
Run: `npm run lint`
Expected: không lỗi mới.

- [ ] **Step 5: Commit**

```bash
git add components/GoogleSignInButton.tsx app/login/page.tsx app/register/page.tsx
git commit -m "feat(auth): Google sign-in button on login and register pages"
```

---

### Task 5: Dark mode — ThemeProvider + layout

**Files:**
- Create: `components/ThemeProvider.tsx`
- Modify: `app/layout.tsx` (bọc `<ThemeProvider>`, thêm `suppressHydrationWarning`)

**Interfaces:**
- Produces: `ThemeProvider` (named export) — wrapper quanh `next-themes` `ThemeProvider`, nhận mọi prop của nó.

- [ ] **Step 1: Tạo `components/ThemeProvider.tsx`**

```tsx
"use client";

import { ThemeProvider as NextThemesProvider } from "next-themes";
import type { ComponentProps } from "react";

export function ThemeProvider({
  children,
  ...props
}: ComponentProps<typeof NextThemesProvider>) {
  return <NextThemesProvider {...props}>{children}</NextThemesProvider>;
}
```

- [ ] **Step 2: Bọc layout + thêm `suppressHydrationWarning`**

Trong `app/layout.tsx`:

Thêm import sau `import { Toaster } ...`:
```tsx
import { ThemeProvider } from "@/components/ThemeProvider";
```

Thêm `suppressHydrationWarning` vào thẻ `<html>` (dòng 39-42) — thêm attribute vào thẻ mở:
```tsx
    <html
      lang="vi"
      suppressHydrationWarning
      className={`${geistSans.variable} ${geistMono.variable} ${beVietnamPro.variable} ${lora.variable} h-full antialiased`}
    >
```

Thay thân `<body>` (dòng 43-46) thành:
```tsx
      <body className="min-h-full flex flex-col">
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
```

- [ ] **Step 3: Build để xác nhận không lỗi + không FOUC config**

Run: `npx tsc --noEmit`
Expected: không lỗi.
Run: `npm run build`
Expected: build thành công (kiểm tra `ThemeProvider` render đúng, không lỗi hydration ở output).

- [ ] **Step 4: Kiểm thủ công (ghi lại kết quả trong report)**

Chạy `npm run dev`, mở trang chủ:
- DevTools → set OS theme = dark → reload → nền tối ngay, **không nháy trắng**.
- Không có warning hydration trong console.
(Nếu không chạy được dev trong môi trường subagent, ghi rõ "chưa kiểm thủ công — cần người dùng xác nhận" trong report.)

- [ ] **Step 5: Commit**

```bash
git add components/ThemeProvider.tsx app/layout.tsx
git commit -m "feat(theme): next-themes ThemeProvider in root layout"
```

---

### Task 6: ThemeToggle + gắn vào Navbar

**Files:**
- Create: `components/ThemeToggle.tsx`
- Modify: `components/Navbar.tsx` (chèn `<ThemeToggle />`, hiện cho mọi người)

**Interfaces:**
- Consumes: `ThemeProvider` context (Task 5), `components/ui/dropdown-menu`, `components/ui/button`.
- Produces: `ThemeToggle` (default export).

- [ ] **Step 1: Tạo `components/ThemeToggle.tsx`**

```tsx
"use client";

import * as React from "react";
import { Moon, Sun, Monitor } from "lucide-react";
import { useTheme } from "next-themes";
import { Button } from "@/components/ui/button";
import {
  DropdownMenu,
  DropdownMenuTrigger,
  DropdownMenuContent,
  DropdownMenuItem,
} from "@/components/ui/dropdown-menu";

export default function ThemeToggle() {
  const { setTheme } = useTheme();
  const [mounted, setMounted] = React.useState(false);
  React.useEffect(() => setMounted(true), []);

  // Tránh hydration mismatch: chỉ render nội dung phụ thuộc theme sau khi mounted.
  if (!mounted) {
    return (
      <Button variant="ghost" size="icon" className="h-9 w-9" aria-label="Đổi giao diện" />
    );
  }

  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button variant="ghost" size="icon" className="h-9 w-9" aria-label="Đổi giao diện">
          <Sun className="h-5 w-5 dark:hidden" />
          <Moon className="hidden h-5 w-5 dark:block" />
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end">
        <DropdownMenuItem onClick={() => setTheme("light")}>
          <Sun className="mr-2 h-4 w-4" /> Sáng
        </DropdownMenuItem>
        <DropdownMenuItem onClick={() => setTheme("dark")}>
          <Moon className="mr-2 h-4 w-4" /> Tối
        </DropdownMenuItem>
        <DropdownMenuItem onClick={() => setTheme("system")}>
          <Monitor className="mr-2 h-4 w-4" /> Hệ thống
        </DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  );
}
```

- [ ] **Step 2: Chèn `<ThemeToggle />` vào Navbar**

Trong `components/Navbar.tsx`:

Thêm import sau `import { NavLinks, MobileNavLinks } ...` (dòng 8):
```tsx
import ThemeToggle from "@/components/ThemeToggle";
```

Đặt toggle hiện cho mọi người (cả khi chưa đăng nhập): trong `<nav className="flex items-center gap-2">` (dòng 29), thêm `<ThemeToggle />` ngay sau thẻ mở `<nav ...>` và trước `{loggedIn ? (`:
```tsx
        <nav className="flex items-center gap-2">
          <ThemeToggle />
          {loggedIn ? (
```

- [ ] **Step 3: Typecheck + build**

Run: `npx tsc --noEmit`
Expected: không lỗi.
Run: `npm run build`
Expected: build thành công.

- [ ] **Step 4: Kiểm thủ công (ghi lại trong report)**

Chạy `npm run dev`:
- Nút toggle hiện ở Navbar (cả trang đã/chưa đăng nhập).
- Chọn Sáng/Tối/Hệ thống → giao diện đổi ngay.
- Reload → giữ nguyên lựa chọn (localStorage), không nháy.
- Duyệt dashboard/jobs ở dark mode → chữ và nền đọc được (dùng token nên tự thích ứng).
- Xác nhận trang xem trước CV (`/cv/[id]`) **vẫn nền sáng** — đúng thiết kế, không phải lỗi.
(Nếu không chạy được dev, ghi "chưa kiểm thủ công — cần người dùng xác nhận".)

- [ ] **Step 5: Commit**

```bash
git add components/ThemeToggle.tsx components/Navbar.tsx
git commit -m "feat(theme): ThemeToggle dropdown in navbar (light/dark/system)"
```

---

## Ghi chú kiểm thử tổng (sau tất cả task)

- **OAuth cần credentials thật** để test end-to-end: điền `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` vào `.env`, chạy `npm run dev`, bấm "Đăng nhập với Google":
  - Email mới → tạo tài khoản Ứng viên, vào `/dashboard`.
  - Email đã có (đăng ký mật khẩu) → liên kết, giữ role cũ.
  - Tài khoản chỉ-Google thử đăng nhập bằng mật khẩu ở `/login` → bị từ chối.
- **Dark mode:** không có unit test (đúng theo spec — UI/visual); dựa vào kiểm thủ công ở Task 5 & 6.
