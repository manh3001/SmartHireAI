# Tag "Hot" + thống kê tin — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thêm badge "Hot" (≥3 lượt ứng tuyển) trên thẻ việc và hiện số lượt ứng tuyển mỗi tin cho nhà tuyển dụng.

**Architecture:** Mở rộng helper thuần `jobBadges` với `applicationCount` + ngưỡng `HOT_APPLICATIONS`; đưa số lượt ứng tuyển vào nguồn dữ liệu thẻ việc (`/jobs` qua subquery raw SQL trong `JobRow`; trang chủ qua prisma `_count`); recruiter dashboard hiện count qua `_count`. Không schema mới.

**Tech Stack:** Next.js App Router (ĐỌC guide trong `node_modules/next/dist/docs/` trước khi viết), React server components, Prisma 6, Tailwind, vitest.

## Global Constraints

- Không schema mới/migration/AI. Đếm từ `Application` (`jobId`) sẵn có.
- `HOT_APPLICATIONS = 3`; "Hot" khi `applicationCount >= HOT_APPLICATIONS`. Thứ tự badge: **Hot → Mới → Lương cao**.
- Tiếng Việt; Tailwind tokens; prisma default import `@/lib/db/prisma`.
- Baseline 408 tests phải giữ xanh. Test: `npx vitest run <path>`; typecheck: `npx tsc --noEmit`.
- `/jobs/saved` và `/jobs/recommendations` KHÔNG thuộc phạm vi (không hiện Hot).

---

### Task 1: Mở rộng `jobBadges` + render "Hot" trên thẻ

**Files:**
- Modify: `lib/jobs/job-badges.ts`
- Modify: `lib/jobs/__tests__/job-badges.test.ts`
- Modify: `components/JobCard.tsx`

**Interfaces:**
- Produces:
  - `JobBadge.tone` gồm `"hot" | "new" | "salary"`
  - `export const HOT_APPLICATIONS = 3`
  - `jobBadges(job: { createdAt?: string | Date | null; salaryMax?: number | null; applicationCount?: number | null }, now?: Date): JobBadge[]`

- [ ] **Step 1: Thêm test thất bại**

Thêm vào `lib/jobs/__tests__/job-badges.test.ts` (trong `describe("jobBadges", ...)`), và cập nhật import dòng 2:
```ts
import { jobBadges, HIGH_SALARY_VND, HOT_APPLICATIONS } from "../job-badges";
```
```ts
  it("applicationCount >= nguong -> badge Hot", () => {
    const b = jobBadges({ applicationCount: HOT_APPLICATIONS }, NOW);
    expect(b).toEqual([{ label: "Hot", tone: "hot" }]);
  });

  it("applicationCount duoi nguong hoac thieu -> khong Hot", () => {
    expect(jobBadges({ applicationCount: HOT_APPLICATIONS - 1 }, NOW).some((x) => x.tone === "hot")).toBe(false);
    expect(jobBadges({}, NOW).some((x) => x.tone === "hot")).toBe(false);
  });

  it("ca ba dieu kien -> thu tu Hot, Moi, Luong cao", () => {
    const b = jobBadges(
      { applicationCount: 5, createdAt: new Date("2026-09-10T00:00:00Z"), salaryMax: 50_000_000 },
      NOW,
    );
    expect(b).toEqual([
      { label: "Hot", tone: "hot" },
      { label: "Mới", tone: "new" },
      { label: "Lương cao", tone: "salary" },
    ]);
  });
```

- [ ] **Step 2: Chạy test — phải fail**

Run: `npx vitest run lib/jobs/__tests__/job-badges.test.ts`
Expected: FAIL (`HOT_APPLICATIONS` chưa export / chưa có badge Hot).

- [ ] **Step 3: Cập nhật `lib/jobs/job-badges.ts`**

Thay toàn bộ file bằng:
```ts
export type JobBadge = { label: string; tone: "hot" | "new" | "salary" };

export const HIGH_SALARY_VND = 40_000_000;
export const NEW_JOB_DAYS = 7;
export const HOT_APPLICATIONS = 3;

const DAY_MS = 24 * 60 * 60 * 1000;

export function jobBadges(
  job: { createdAt?: string | Date | null; salaryMax?: number | null; applicationCount?: number | null },
  now: Date = new Date(),
): JobBadge[] {
  const badges: JobBadge[] = [];

  if (job.applicationCount != null && job.applicationCount >= HOT_APPLICATIONS) {
    badges.push({ label: "Hot", tone: "hot" });
  }

  if (job.createdAt != null) {
    const created = new Date(job.createdAt).getTime();
    const ageDays = (now.getTime() - created) / DAY_MS;
    if (ageDays >= 0 && ageDays <= NEW_JOB_DAYS) {
      badges.push({ label: "Mới", tone: "new" });
    }
  }

  if (job.salaryMax != null && job.salaryMax >= HIGH_SALARY_VND) {
    badges.push({ label: "Lương cao", tone: "salary" });
  }

  return badges;
}
```

- [ ] **Step 4: Chạy test — phải pass**

Run: `npx vitest run lib/jobs/__tests__/job-badges.test.ts`
Expected: PASS (các test cũ + 3 test mới).

- [ ] **Step 5: Thêm `applicationCount` + màu "hot" trong `JobCard.tsx`**

Trong `components/JobCard.tsx`, thêm field vào `JobCardData` (ngay dưới `createdAt?: string | Date | null;`):
```ts
  applicationCount?: number | null;
```
Thay khối class ternary của badge (dòng `b.tone === "new" ? ... : ...`) bằng lookup 3 tone. Đổi block render badge thành:
```tsx
          {badges.length > 0 && (
            <div className="mt-1 flex flex-wrap gap-1">
              {badges.map((b) => (
                <span
                  key={b.label}
                  className={
                    "rounded-full px-2 py-0.5 text-[11px] font-medium " +
                    (b.tone === "hot"
                      ? "bg-rose-500/10 text-rose-600 dark:text-rose-400"
                      : b.tone === "new"
                        ? "bg-emerald-500/10 text-emerald-600 dark:text-emerald-400"
                        : "bg-amber-500/10 text-amber-600 dark:text-amber-400")
                  }
                >
                  {b.label}
                </span>
              ))}
            </div>
          )}
```

- [ ] **Step 6: Typecheck + test toàn bộ**

Run: `npx tsc --noEmit` → Expected: không lỗi.
Run: `npx vitest run` → Expected: toàn bộ PASS (408 + 3 = 411).

- [ ] **Step 7: Commit**

```bash
git add lib/jobs/job-badges.ts lib/jobs/__tests__/job-badges.test.ts components/JobCard.tsx
git commit -m "feat(jobs): badge Hot (>=3 lượt ứng tuyển) trên thẻ việc"
```

---

### Task 2: Số lượt ứng tuyển vào nguồn dữ liệu thẻ (/jobs + trang chủ)

**Files:**
- Modify: `lib/jobs/search-query.ts` (COLS)
- Modify: `lib/jobs/search.ts` (`JobRow`)
- Modify: `lib/jobs/__tests__/search-query.test.ts` (test)
- Modify: `app/page.tsx` (latestJobs `_count` + map)

**Interfaces:**
- Consumes: `applicationCount` field trên `JobCardData` (Task 1)
- Produces: `JobRow.applicationCount: number`

- [ ] **Step 1: Thêm test thất bại (SQL có cột applicationCount)**

Thêm vào `lib/jobs/__tests__/search-query.test.ts` (trong `describe("buildSearchSql", ...)`):
```ts
  it("select gom applicationCount qua subquery COUNT Application", () => {
    const { sql } = buildSearchSql({ limit: 20 });
    expect(sql).toContain(`AS "applicationCount"`);
    expect(sql).toContain(`FROM "Application"`);
  });
```

- [ ] **Step 2: Chạy test — phải fail**

Run: `npx vitest run lib/jobs/__tests__/search-query.test.ts`
Expected: FAIL (COLS chưa có applicationCount).

- [ ] **Step 3: Thêm cột vào COLS**

Trong `lib/jobs/search-query.ts`, sửa hằng `COLS` — thêm subquery vào cuối chuỗi (trước dấu backtick đóng):
```ts
const COLS =
  `id, title, company, "rawText", "createdAt", location, "employmentType", "experienceLevel", skills, "salaryMin", "salaryMax", "salaryNegotiable", category, ` +
  `(SELECT COUNT(*)::int FROM "Application" WHERE "Application"."jobId" = "JobDescription".id) AS "applicationCount"`;
```

- [ ] **Step 4: Thêm field vào `JobRow`**

Trong `lib/jobs/search.ts`, thêm vào type `JobRow` (cuối, trước `}`):
```ts
  applicationCount: number;
```

- [ ] **Step 5: Chạy test — phải pass**

Run: `npx vitest run lib/jobs/__tests__/search-query.test.ts`
Expected: PASS.

- [ ] **Step 6: Trang chủ — đếm applications cho latestJobs**

Trong `app/page.tsx`, trong `prisma.jobDescription.findMany` của `latestJobs`, thêm vào `select` (cùng cấp các field khác):
```ts
        _count: { select: { applications: true } },
```
Trong JSX render "Việc làm mới nhất", đổi truyền prop:
```tsx
                {latestJobs.map((j) => (
                  <JobCard key={j.id} job={{ ...j, applicationCount: j._count.applications }} href={loggedIn ? `/jobs/${j.id}` : "/login"} />
                ))}
```

- [ ] **Step 7: Typecheck + test toàn bộ**

Run: `npx tsc --noEmit` → Expected: không lỗi.
Run: `npx vitest run` → Expected: toàn bộ PASS (411 + 1 = 412).

- [ ] **Step 8: Commit**

```bash
git add lib/jobs/search-query.ts lib/jobs/search.ts lib/jobs/__tests__/search-query.test.ts app/page.tsx
git commit -m "feat(jobs): đếm lượt ứng tuyển cho thẻ việc (/jobs raw SQL + trang chủ)"
```

---

### Task 3: Recruiter dashboard — số lượt ứng tuyển mỗi tin

**Files:**
- Modify: `app/dashboard/page.tsx` (nhánh RECRUITER)

- [ ] **Step 1: Thêm `_count` vào query tin của recruiter**

Trong `app/dashboard/page.tsx`, nhánh `if (isRecruiter)`, query `prisma.jobDescription.findMany` — thêm vào `select`:
```ts
      _count: { select: { applications: true } },
```

- [ ] **Step 2: Hiện "N ứng tuyển" cạnh ngày**

Trong cùng nhánh, dòng meta hiển thị company + ngày:
```tsx
                      <div className="text-xs text-muted-foreground">
                        {j.company || "—"} · {new Date(j.createdAt).toLocaleDateString("vi-VN")}
                      </div>
```
đổi thành:
```tsx
                      <div className="text-xs text-muted-foreground">
                        {j.company || "—"} · {new Date(j.createdAt).toLocaleDateString("vi-VN")} · {j._count.applications} ứng tuyển
                      </div>
```

- [ ] **Step 3: Typecheck + test toàn bộ**

Run: `npx tsc --noEmit` → Expected: không lỗi.
Run: `npx vitest run` → Expected: toàn bộ PASS (412, không đổi).

- [ ] **Step 4: Commit**

```bash
git add app/dashboard/page.tsx
git commit -m "feat(dashboard): hiện số lượt ứng tuyển mỗi tin cho recruiter"
```

---

### Task 4: Kiểm chứng cuối

**Files:** (không sửa; chỉ chạy)

- [ ] **Step 1: Lint + typecheck + test đầy đủ**

Run:
```bash
npx tsc --noEmit
npm run lint
npx vitest run
```
Expected: tsc sạch; vitest 412 PASS. Lint: KHÔNG có lỗi MỚI so với baseline (đã tồn đọng sẵn 3 errors: ThemeToggle set-state-in-effect, recruiter-analytics.test no-explicit-any x2 — ngoài phạm vi). Nếu có lỗi lint ở file vòng này sửa, phải sửa.

- [ ] **Step 2: Chạy app soát mắt (khuyến nghị)**

Run: `npm run dev`:
- `/jobs` và trang chủ: tin có ≥3 lượt ứng tuyển hiện badge "Hot" (đỏ) cùng "Mới"/"Lương cao" đúng thứ tự.
- `/dashboard` (recruiter): mỗi tin hiện "· N ứng tuyển".

## Self-Review

- **Spec coverage:** jobBadges + Hot + thứ tự (Task 1) ✅ · JobCard render Hot + JobCardData.applicationCount (Task 1) ✅ · /jobs raw SQL applicationCount (Task 2) ✅ · trang chủ _count (Task 2) ✅ · recruiter dashboard count (Task 3) ✅ · unit test Hot + SQL (Task 1,2) ✅ · kiểm chứng cuối (Task 4) ✅.
- **Placeholder scan:** không có TBD/TODO; mọi step có code đầy đủ.
- **Type consistency:** `HOT_APPLICATIONS`, `JobBadge.tone` ("hot"|"new"|"salary"), `applicationCount` dùng nhất quán ở jobBadges/JobCardData/JobRow; trang chủ map `_count.applications` → `applicationCount`; recruiter dùng `_count.applications`.
